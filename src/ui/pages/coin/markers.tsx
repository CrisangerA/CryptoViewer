import React from 'react';
import Page from '@components/layout/Page';
import ListView from '@components/coin/ListView';

export default function CoinsMarkersPage() {
  return (
    <Page>
      <ListView />
    </Page>
  );
}
